# Legacy datasets archive

- `datasets/` and `rust_code/datasets/` were moved here on archive to avoid duplication.
- Canonical data now lives under `data/processed/` (see `data/README.md`).
- Keep these files for reference only; do not use them as sources for new work.
